<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Login.Views.MainPage">

    <StackLayout>
        <Entry Placeholder="Login" x:Name="EntryLogin"/>
        <Entry Placeholder="password" x:Name="EntryPassword" IsPassword="True"/>
        <Button Text="Save" Clicked="Saved"/>
        <Button Text="Login" Clicked="logined"/>


        <ListView x:Name="Logins">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Login}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
